{
  "version": 2,
  "outputCapture": "direct-nodejs",
  "$schema": "http://json-schema.org/schema",
  "title": "Forge application tunnel target",
  "description": "Prepare and run a local Forge tunnel development loop.",
  "type": "object",
  "properties": {
    "buildOutputPath": {
      "type": "string",
      "description": "Advanced override for the Forge app build output directory when auto-detection cannot resolve it."
    },
    "debug": {
      "type": "boolean",
      "description": "Run Forge tunnel in debug mode.",
      "alias": "d",
      "default": false
    },
    "verbose": {
      "type": "boolean",
      "description": "Run Forge tunnel in verbose mode.",
      "alias": "v",
      "default": false
    }
  }
}
